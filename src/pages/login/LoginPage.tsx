import { Header } from "../../layouts/Header";
import { LoginTabs } from "../../modules/login/LoginTabs";

export const LoginPage = () => {
    return (
        <div>
            <Header />
            <LoginTabs />
        </div>

    );
};